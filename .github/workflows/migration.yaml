name: migration

on:
  push:
    branches:
      - main
    paths:
      - db/**
      - .github/workflows/migration.yaml
jobs:
  migration:
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v3
      - uses: docker/setup-buildx-action@v2
      - uses: google-github-actions/auth@v0.4.0
        with:
          credentials_json: "${{ secrets.GOOGLE_CREDENTIALS }}"
      - uses: docker/build-push-action@v3
        with:
          push: false
          tags: bizbuzim/migrate:latest
          file: cmd/tools/migration/Dockerfile
          load: true
      - run: |
          gcloud sql connect bizbuzim-production &
          docker run -e POSTGRES_HOST=${{ secrets.POSTGRES_HOST_PRODUCTION }} -e PGPASSWORD=${{ secrets.PGPASSWORD_PRODUCTION }} -e POSTGRESQL_URL=${{ secrets.POSTGRESQL_URL_PRODUCTION }} bizbuzim/migrate
